export default defineAppConfig({
    ui: {
        primary: 'linear-gradient(90deg, #242848 0%, #5761AE 100%)',
        button: {
            default: {
                color: 'primary'
            }
        }
    },

})